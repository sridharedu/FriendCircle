spring:
  application:
    name: user-service
  # TODO: Add database configuration
  # TODO: Add security configuration
  # TODO: Add service discovery configuration

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health,metrics,info
  endpoint:
    health:
      show-details: always